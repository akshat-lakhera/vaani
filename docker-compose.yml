services:
  vaani:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      VAANI_DATA_DIR: /app/data
      VAANI_HOST: 0.0.0.0
      VAANI_PORT: 8080
      TOKENIZERS_PARALLELISM: "false"
